import streamlit as st

# Title of the app
st.title("My First Streamlit App")

# Adding some text
st.write("Hello, this is a simple test app to check my GitHub connection.")

# Creating a simple slider
slider_val = st.slider("Select a value", 0, 100)

# Displaying the selected slider value
st.write(f"The selected value is: {slider_val}")

